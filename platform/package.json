{
  "name": "carrier-iprn-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "postbuild": "test -f dist/ami.js && test -f dist/server.js || (echo 'ERROR: dist build incomplete (missing dist/ami.js — ensure src/ami.ts exists and tsc succeeded)' >&2; exit 1)",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "asterisk-ami-client": "^1.1.5",
    "fastify": "^5.2.1",
    "mysql2": "^3.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
